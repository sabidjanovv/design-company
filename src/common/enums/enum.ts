export enum AdminRole {
  SUPERADMIN = 'superadmin',
  ADMIN = 'admin',
}

export enum Sort {
  ASC = 'ASC',
  DESC = 'DESC',
}

export enum CategoryType {
  INTERIOR = 'interior',
  EXTERIOR = 'exterior',
}

export enum Language {
  UZ = 'uz',
  RU = 'ru',
  EN = 'en',
}